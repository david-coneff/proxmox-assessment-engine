# Homelab as First-Class Environment

Homelab deployments are valid deployments.

The architecture should not assume enterprise scale.

The same principles should apply across scales.

Complexity should scale with need.
