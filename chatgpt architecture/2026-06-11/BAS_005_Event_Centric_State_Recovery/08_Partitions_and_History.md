# Partitions and History

Members may become isolated.

Isolation should not invalidate local history.

When connectivity returns:

History
    -> Exchange
    -> Understanding
    -> Reconciliation

The architecture assumes temporary divergence is normal.
