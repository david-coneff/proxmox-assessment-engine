# Partition Tolerance

Network partitions are expected.

The architecture assumes:

Partitions occur.
Links fail.
Members disappear temporarily.

Governance should degrade gracefully.

Isolation should trigger reconciliation later rather than immediate failure.
