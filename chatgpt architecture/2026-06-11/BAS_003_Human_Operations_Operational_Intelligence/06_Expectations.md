# Expectations

Operators frequently need to know:

What is normal?

The architecture should support generation of expectations.

Examples:

Expected reboot duration.

Expected backup duration.

Expected synchronization duration.

Expected recovery duration.

Expectations reduce uncertainty.
