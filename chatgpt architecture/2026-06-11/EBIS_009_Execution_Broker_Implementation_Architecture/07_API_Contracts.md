Execution:

POST /executions
GET /executions/{id}
POST /executions/{id}/cancel
POST /executions/{id}/abort

Observations:

GET /executions/{id}/observations
GET /executions/{id}/progress

Retention:

POST /retention/checkpoints
POST /retention/protect
POST /retention/permanent
POST /retention/archive
POST /retention/release
