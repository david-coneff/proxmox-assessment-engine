# Expectation Presentation

Expected behavior should be visible before actions are initiated.

Examples:

Expected backup duration.

Expected reboot duration.

Expected synchronization duration.

The architecture favors informed participation.
