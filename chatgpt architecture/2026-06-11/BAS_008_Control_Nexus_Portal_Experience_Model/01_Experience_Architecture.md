# Experience Architecture

Broodforge serves multiple participant types.

The architecture separates experiences rather than assuming a single interface.

Primary experiences:

Control Nexus
Portal

Both consume the same governance ecosystem.
