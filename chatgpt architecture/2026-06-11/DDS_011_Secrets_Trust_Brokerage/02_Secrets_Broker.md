# Secrets Broker

The Secrets Broker manages access to protected secrets.

Examples:

Credentials

Keys

Tokens

Certificates

Protected configuration

The broker governs access rather than ownership.
